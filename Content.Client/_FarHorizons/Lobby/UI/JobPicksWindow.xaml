<DefaultWindow
    xmlns="https://spacestation14.io"
    SetSize="500 400">
    <!-- Starlight, no factions, so just all the jobs. -->
    <ScrollContainer Name="JobList" HorizontalExpand="True" VerticalExpand="True">
        <BoxContainer Name="JobListContent" Orientation="Vertical" HorizontalExpand="True" Margin="0 0 5 0"/>
    </ScrollContainer>
</DefaultWindow>
