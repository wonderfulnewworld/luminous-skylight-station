<DefaultWindow
    xmlns="https://spacestation14.io"
    SetSize="500 400">
    <ScrollContainer Name="JobList" VerticalExpand="True"/> <!-- Starlight, no factions, so just all the jobs. -->
</DefaultWindow>
